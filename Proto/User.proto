syntax = "proto3";

package user;

import "types.proto";

message LoginReq {
	option (types.message_id) = kLoginReq;
	string username = 1;
	string password = 2;
}

message LoginRes {
	option (types.message_id) = kLoginReq;
	
	enum Result {
		kSuccess = 0;
		kFailure = 1;
	}

	Result result = 1;
}